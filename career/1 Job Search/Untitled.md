```js
//actions

const action = () => {
    
    return = (dispatch)=>{
        fetch(team, {})
        .then()
        .then(data =>
             dispatch({
            type: 'TEAM',
            payload: {team: data}
        })
    }
        )
       
}
```

```js
//reducer
const reducer = ({}) => {
    
}
```

```jsx
//file
class Component {
    render(){
        return (
        	<div></div>
        )
    }
}

const maptostatprops = state => {
    
}
export default connect(mapTotStateProps, {action})(Component())
```

```
fetch => {payload:{key: value} type: 'string'} 1.) 
reducer(fetch) => type 2.)
file <=(reducer) 1.)

```

deliverables 